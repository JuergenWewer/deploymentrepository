- hosts: localhost
  tasks:
    - name: Install dynamic storage provisioner
      import_tasks: ./dynamicStorageProvisioner.yml
    - name: create infrastructure and waiting suppression ending
      import_tasks: ./updateTask.yml
